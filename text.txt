// Create a jumbotron with a moment function giving MMMM-Do-YYYY hh:mm:ss; 
Dynamically change background if pastTime < currentTime
Dynamically change background if futureTime > currentTime.
Set localStorage to ensure item is saved. 
Get localStorage

moment().format('MMMM Do YYYY, h:mm:ss a'); Format for current time. 
// Need to add to text in jumbotron

